# خارطة الطريق — إعادة بناء القوائم بالكامل

## ✅ المرحلة ١ (منجزة الآن): هيكل التنقّل الكامل
- قائمة علوية (Mega Menu) بكل الأقسام والتسميات التي طلبتها حرفياً: فتح بطاقات، ادخال الحركات
  المحاسبية، تقارير محاسبة، ادخال حركة المواد، تقارير مستودعية، الملحقات، التصنيع، خدمات،
  اعدادات البرنامج، خروج.
- القائمة الجانبية اليمنى أعيد ترتيبها بنفس التسميات التي طلبتها (ادخال فواتير، اخراج فواتير،
  كشف يومية مستودع، جرد مستودع، دليل المواد، كشف الحركة اليومية، يومية صندوق، إيصال قبض،
  كشف الحساب، سند قيد مركب، أرصدة الحسابات، أمر صرف/قبض، الاستعلام عن مادة...).
- كل عنصر يفتح: (أ) صفحة حقيقية موجودة فعلاً بالنظام، أو (ب) صفحة "قيد الإنشاء" واضحة —
  وليست زراً وهمياً صامتاً. هذا يميّز الميزات الجاهزة عن غير الجاهزة بدل تزييف الكل.
- ميزتان حقيقيتان جديدتان تعملان الآن فعلياً: **آلة حاسبة** (اعدادات البرنامج) و **حول البرنامج**.

## 🔜 المراحل القادمة (كل مرحلة = وحدة كاملة: جداول قاعدة بيانات + شاشة إدخال + تقرير)
كل مرحلة تحتاج مني كتابة SQL جديد (تشغّله أنت بمحرر SQL بلوحة Supabase) + دوال DB.js + صفحة UI حقيقية.

1. **بطاقة الزبون + إيصالات/أوامر القبض والدفع** — الأهم، لأنها أساس أغلب التقارير المطلوبة
   (متابعة المبيعات، كشف الحساب، الفواتير المستحقة). تحتاج جدول `customers` + توسيع
   `journal_entries` أو جدول جديد `receipts_payments`.
2. **بطاقة العملة (تعدد العملات)** — النظام حالياً دينار عراقي فقط بدون كسور. دعم عملة ثانية
   يتطلب سعر صرف تاريخي بكل قيد — قرار تصميمي يحتاج نقاشاً قبل البناء.
3. **بطاقة المشروع / الفرع / المنطقة / الصنف الرئيسي / تشابه المواد / توليد المواد الجماعي** —
   جداول تصنيف بسيطة نسبياً، يمكن بناؤها بمرحلة واحدة.
4. **التقارير المستودعية والمحاسبية الموسّعة** (كشف تحليلي/تجميعي/احصائي، جرد بالأرقام
   التسلسلية، دليل مراكز الكلفة) — تُبنى فوق البيانات الموجودة أساساً، الأسهل تنفيذاً.
5. **طلبيات البيع والشراء + يومية المبيعات + ايصالات الشحن** — تحتاج جدول طلبيات وربطه
   بالفواتير الحالية.
6. **سندات الديون + كشوفاتها** — جدول جديد + تقرير.
7. **الملحقات (عقود/أرشيف/مهام/تأجير) والتصنيع والخدمات** — هذه أنظمة فرعية منفصلة كل
   واحدة بحجم النظام الحالي تقريباً؛ تحتاج نقاشاً منفصلاً هل فعلاً مطلوبة لعملك، أم أنها
   بنود من قائمة برنامج عام لا تنطبق على نشاطك.
8. **اعدادات البرنامج المتبقية** (بريد داخلي، أرشفة وثائق، صيانة ملفات، تصميم باركود،
   مزامنة بين فروع، استيراد إكسل) — ملاحظة: "تنفيذ أوامر SQL Server" غير قابلة للتطبيق
   على Supabase/Postgres ولن أبنيها لأسباب أمنية (لا يجوز تنفيذ SQL حر من المتصفح).

## كيف نكمل؟
أخبرني أي مرحلة تريدها أولاً (الأرجح: المرحلة ١ من القادمة — الزبائن وإيصالات/أوامر القبض
والدفع، لأنها الأكثر تأثيراً)، وسأبدأ ببناء الجداول والشاشات الحقيقية لها.
